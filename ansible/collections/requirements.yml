---
# Ansible Collection Requirements — IBM BAW Automation
# Install: ansible-galaxy collection install -r ansible/collections/requirements.yml

collections:
  - name: kubernetes.core
    version: ">=2.4.0"
  - name: community.crypto
    version: ">=2.15.0"
  - name: ansible.posix
    version: ">=1.5.0"
  - name: awx.awx
    version: ">=23.0.0"
